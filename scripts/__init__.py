"""Re-runnable diagnostic + utility scripts.

Each module here is invokable from the command line (e.g.
``python scripts/fetch_historical_bars.py --all-trades``) and is
importable as ``scripts.<name>`` so test modules can exercise
helper functions directly without spawning a subprocess.
"""
